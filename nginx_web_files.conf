# Добавить после location /storage/instruktagi/
    
    # Публичные файлы (PDF и другие документы)
    location ^~ /web/files/ {
        alias /home/instr/app/web/files/;
        autoindex off;
        # Разрешить доступ к PDF
        types {
            application/pdf pdf;
        }
    }
